class Room(object):
    # set as none to allow inheriting classes to specify its value
    room_capacity = None

    def __init__(self, room_id, name):
        self.room_id = person_id
        self.name = name
